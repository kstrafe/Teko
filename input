(define x (fn () 1))
(x)
(define zawa (fn (a) (+ a 1)))
(define zaka (fn (a) (zawa a)))
(define kek (mo - (symbol zaka)))

(write (" before))
(define max 100000000)
(define v 1)
(define count (fn (a)
                  (if (>= a max)
                      a
                      (count (+ v a)))))
(count 0)
(write (" after max))
