(write (" before))
(define nth (fn (lst acc)
                (if (= acc 0)
                    (head lst)
                    (nth (tail lst) (- acc 1)))))
(write (head (tail (tail (quote a b c d)))))
(write (nth (quote a b c d) 5))
(exit)
(define max 10002)
(define v 100)
(define count (fn (a)
                  (if (not (< a max))
                      a
                      (count (+ v a)))))
(write (count 0))
(write (" after max))
